'''
1. Initialize stack

2. push:
    Check if stack is empty, then append tuple of (val,val)
    Else, if new val is less than prev min, append (val,val), else append (val,prev min)

3. pop
    pop stack

4. top
    return first index of last tuple in stack

5. getMin
    return second index of last tuple in stack

'''

