'''
Use similar logic from Remove element
1. Initialize j = 0, and then a for loop through indices of nums
2. If the current ith value is not 0, then ith and jth index values switch because j tracks the nonzero values
3. Increment j 

'''

